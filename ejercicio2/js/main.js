$(function(){ //funcion "Ready" ejecuta unicamente cuando ya se renderizo el codigo html java scrip es un archivo que interactua con html css

    lightbox.option({
  albumLabel: 'Imagen%1 de %2',
      })
});